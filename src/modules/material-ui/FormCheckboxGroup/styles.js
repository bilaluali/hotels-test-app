import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(unusedTheme => ({
    root: {},
    formLabel: {},
    formGroup: {},
    formControlLabel: {},
    checkbox: {}
}));

export default useStyles;