import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(unusedTheme => ({
    root: {},
    cardImage: {
        margin: 0
    },
    cardContent: {
        paddingTop: 5,
    }
}));

export default useStyles;