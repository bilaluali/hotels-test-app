import { makeStyles } from '@mui/styles';

const useStyles = makeStyles(unusedTheme => ({
    root: {
        padding: [[10, 0, 10, 0]],
        boxShadow: '0 0 20px rgb(0 0 0 / 35%) !important'
    }
}));

export default useStyles;